{
  "name": "dragon-shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "copy": "cpy src/**/* ../frontend/shared/ && cpy src/*/* ../backend/shared/",
    "clean": "rimraf ../frontend/shared && rimraf ../backend/shared ",
    "build": "npm run clean && npm run copy"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "typescript": "^4.0.0",
    "rimraf": "^5.0.0"
  }
}